# Kafka Samples

This folder contains sample topic definitions, a CDC connector configuration, and an Avro schema for AI modernization event flows.

Production Kafka environments should use authentication, TLS, ACLs, schema compatibility rules, dead-letter handling, and operational dashboards.
