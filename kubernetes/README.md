# Kubernetes Samples

Sample manifests for an LLM gateway deployment that centralizes policy, model routing, retrieval orchestration, and telemetry.

Use sealed secrets, external secrets, or a cloud secret manager in production. Do not commit provider keys or model credentials.
